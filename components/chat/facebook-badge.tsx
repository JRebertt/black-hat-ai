"use client"

interface FacebookBadgeProps {
  isSelected: boolean
  onToggle: () => void
}

export function FacebookBadge({ isSelected, onToggle }: FacebookBadgeProps) {
  return null // Removido para design mais limpo
}
